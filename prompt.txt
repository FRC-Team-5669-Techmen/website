Make three site improvements. First read about.html, the other page files that include the shared scripts, scripts/main.js, scripts/season.js, and styles/styles.css so you match the existing structure and styling. Use the site's existing CSS variables and gold accent throughout. Do not commit or push.

1) Site-wide scroll reveal (subtle).
   - Content blocks fade in and rise slightly (about 8px, ~0.45s ease) as they scroll into view, once each, via an IntersectionObserver.
   - Implement it in a shared script that already loads on every page, or a new scripts/reveal.js added to every page's script includes, so it applies site-wide and not just on About.
   - CRITICAL progressive enhancement: elements must be fully visible by default. Only hide-then-reveal while JavaScript is running. Do this by having the script add a class (for example "reveal-on") to the document root, and scope the start-hidden CSS to that class (for example html.reveal-on .reveal{opacity:0;transform:translateY(8px)}). If JS is disabled or errors, nothing is hidden and the site looks normal.
   - Respect prefers-reduced-motion: reduce by skipping the effect entirely (no hiding, no transform).
   - Target the main content blocks on each page (headings, paragraphs, list items, section groups) inside the main content container. Do not animate the navbar, footer, or loader.

2) Team History as a timeline (about.html).
   - Replace the four Team History <p> paragraphs with a vertical timeline: a vertical line down the left, a gold dot marking each entry, and for each entry a gold year label, a title, and the body text. Use these four entries, in order:

     2015 / Founding
     Founded by Luis Garza, an alumnus and former teacher at Don Bosco Technical Institute. The team debuted at the Los Angeles Regional, finishing 30th of 66, and was built around a five-year plan to grow a new team into a regional playoff contender.

     2016-2019 / The climb
     The team improved steadily over the following seasons, reaching the playoffs at the 2018 Ventura Regional and posting its strongest run yet in 2019, ranking 8th of 49 at the Orange County Regional and again advancing to the playoffs.

     2020 / The reset
     The 2020 season ended early when FIRST suspended competition during the COVID-19 pandemic. Much of the team's experience and momentum was lost, and 5669 found itself rebuilding almost from the ground up.

     2021-2026 / The comeback
     The years since have been a steady climb back, stronger each season. California's move to a district model brought 5669 into the FIRST California District, and in 2026 the team returned to the playoffs as a first-round alliance pick, its best performance in seven years.

   - Keep the <h3>Team History</h3> heading, the FIRST link, and the image. Put the timeline CSS in styles/styles.css under scoped classes, using the gold accent and existing variables: dots on the line, year labels in gold, titles in the light body color, text in the muted color.

3) Count-up on the Achievements stats (scripts/season.js).
   - When the #team-stats numbers scroll into view, animate each from 0 up to its value once (about 900ms, ease-out), using an IntersectionObserver so it fires on scroll-in rather than on load.
   - Resilience: if the animation cannot run (no observer, reduced motion, or any error), show the final numbers immediately. Never leave a number stuck at 0 or blank. Respect prefers-reduced-motion: reduce by showing final values with no count.

When done, tell me which files you changed and confirm the no-JS and reduced-motion fallbacks are in place.